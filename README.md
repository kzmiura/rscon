# RSCON

Source Rcon client in Rust.

## How to use

```bash
rscon --help
```
```text
Usage: rscon --password <PASSWORD> <HOST> <PORT>

Arguments:
  <HOST>  
  <PORT>  

Options:
  -p, --password <PASSWORD>  
  -h, --help                 Print help
  -V, --version              Print version
```
